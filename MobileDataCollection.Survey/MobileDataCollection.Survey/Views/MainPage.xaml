<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MobileDataCollection.Survey"
             x:Class="MobileDataCollection.Survey.Views.MainPage"
             xmlns:converters="clr-namespace:MobileDataCollection.Survey.Converters">

    <ContentPage.Resources>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
        <x:String x:Key="BoolToColorConverterArgs">1,1,1,0.7,0.7,0.7</x:String>
    </ContentPage.Resources>
    
    <StackLayout>
        <ListView x:Name="MenuList" ItemTapped="MenuList_ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout BackgroundColor="{Binding Unlocked, Converter={StaticResource BoolToColorConverter}, ConverterParameter={StaticResource BoolToColorConverterArgs}}"
                                         Orientation="Vertical">
                                <StackLayout Orientation="Horizontal" 
                                             Margin="8,2,8,1">
                                    <Label Text="{Binding ChapterName}"/>
                                    <Label Text="{Binding ProgressString}"
                                           HorizontalOptions="EndAndExpand"/>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
