<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MobileDataCollection.Survey"
             x:Class="MobileDataCollection.Survey.Views.MainPage">

    <StackLayout>
        <ListView x:Name="MenuList" ItemTapped="MenuList_ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout BackgroundColor="{Binding BackgroundColor}"
                                         Orientation="Vertical">
                                <StackLayout Orientation="Horizontal" 
                                             Margin="8,2,8,1">
                                    <Label Text="{Binding ChapterName}"/>
                                    <Label Text="{Binding ProgressString}"
                                           HorizontalOptions="EndAndExpand"/>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
